"#!/bin/bash
# OASIS AI Factory - Hugging Face Spaces Deployment Script

set -e

# Colors
GREEN='\033[0;32m'
BLUE='\033[0;34m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
NC='\033[0m'

echo -e "${BLUE}"
echo "ðŸš€ OASIS AI Factory Deployment"
echo "=============================="
echo -e "${NC}"

# Check if required files exist
echo -e "${BLUE}ðŸ“‹ Checking required files...${NC}"

required_files=("app.py" "requirements.txt")
for file in "${required_files[@]}"; do
    if [[ -f "$file" ]]; then
        echo -e "${GREEN}âœ… $file found${NC}"
    else
        echo -e "${RED}âŒ $file missing${NC}"
        echo -e "${YELLOW}Please ensure $file exists in current directory${NC}"
        exit 1
    fi
done

# Create README.md for HF Spaces
echo -e "${BLUE}ðŸ“ Creating HF Spaces README...${NC}"
cat > README.md << 'EOF'
---
title: OASIS AI Factory v3.0
emoji: ðŸš€
colorFrom: blue
colorTo: purple
sdk: gradio
sdk_version: 4.44.0
app_file: app.py
pinned: false
license: mit
short_description: Ultra-Lightweight Multi-Model AI Platform
tags:
  - oasis
  - ai
  - nlp
  - sentiment-analysis
  - text-generation
  - termux
---

# ðŸš€ OASIS AI Factory v3.0

Ultra-Lightweight Multi-Model AI Platform for OASIS v3 ecosystem.

## Features

- ðŸ˜Š **Sentiment Analysis** - Advanced emotion detection
- ðŸ“„ **Text Summarization** - Intelligent content summarization  
- ðŸŒ **Translation** - English â†” Spanish translation
- âœï¸ **Text Generation** - Creative text completion
- â“ **Question Answering** - Context-based Q&A
- ðŸŽ­ **Emotion Classification** - Multi-emotion detection

## Architecture

- **Termux Command Center** - Ultra-lightweight orchestrator (<1MB)
- **HF Spaces Backend** - Heavy AI processing in the cloud
- **API-First Design** - RESTful communication between layers
- **Zero Dependencies** - No heavy libraries on mobile device

## Usage

1. **Web Interface**: Use the Gradio interface directly
2. **API Access**: Call endpoints programmatically
3. **Termux Orchestration**: Control from OASIS orchestrator

## API Endpoints

- `/api/predict` - Main prediction endpoint
- All Gradio functions available via REST API

## Revenue Model

- AI-as-a-Service offerings
- Custom model implementations  
- Enterprise API access
- Consultation services

Target: $50K+/month through ultra-efficient mobile-first AI orchestration.

## License

MIT License - Part of The New Civilization project.
EOF

echo -e "${GREEN}âœ… README.md created${NC}"

# Validate Python syntax
echo -e "${BLUE}ðŸ” Validating Python syntax...${NC}"
python -m py_compile app.py
echo -e "${GREEN}âœ… Python syntax valid${NC}"

# Display deployment instructions
echo ""
echo -e "${YELLOW}ðŸš€ Deployment Instructions:${NC}"
echo ""
echo -e "${BLUE}1. Create new HF Space:${NC}"
echo "   https://huggingface.co/new-space"
echo ""
echo -e "${BLUE}2. Upload files:${NC}"
echo "   - app.py"
echo "   - requirements.txt"
echo "   - README.md"
echo ""
echo -e "${BLUE}3. Configure Space:${NC}"
echo "   - SDK: Gradio"
echo "   - Python version: 3.10+"
echo "   - Hardware: CPU Basic (free)"
echo ""
echo -e "${BLUE}4. Set Environment Variables:${NC}"
echo "   - HF_TOKEN: ${HF_TOKEN:-your_token_here}"
echo ""
echo -e "${BLUE}5. Deploy and test:${NC}"
echo "   - Space will build automatically"
echo "   - Test all AI functions"
echo "   - Update orchestrator endpoints"
echo ""
echo -e "${GREEN}ðŸŽ‰ Ready for deployment!${NC}""
