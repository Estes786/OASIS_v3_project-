"#!/bin/bash
# OASIS v3 Termux Setup - Ultra-Lightweight Revolution
# STRICT: Only Python + urllib (NO external dependencies)

set -e

# Colors for output
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
PURPLE='\033[0;35m'
CYAN='\033[0;36m'
NC='\033[0m' # No Color

echo -e "${CYAN}"
echo "ðŸš€ OASIS v3 Termux Setup"
echo "========================"
echo -e "${NC}"
echo -e "${GREEN}Ultra-Lightweight AI Orchestration Revolution${NC}"
echo -e "${PURPLE}Zero Heavy Dependencies â€¢ <1MB Footprint${NC}"
echo ""

# Check if running on Termux
if [[ ! "$PREFIX" == *"com.termux"* ]]; then
    echo -e "${YELLOW}âš ï¸  Warning: Not running in Termux environment${NC}"
    echo -e "${BLUE}â„¹ï¸  This script is optimized for Termux${NC}"
fi

# Step 1: Update Termux packages
echo -e "${BLUE}ðŸ“¦ Updating Termux packages...${NC}"
pkg update -y && pkg upgrade -y

# Step 2: Install ONLY essential packages (NO heavy dependencies)
echo -e "${BLUE}âš¡ Installing minimal packages...${NC}"
pkg install -y python git curl wget

# Step 3: Verify Python installation
echo -e "${BLUE}ðŸ” Verifying Python installation...${NC}"
python --version
echo -e "${GREEN}âœ… Python installed successfully${NC}"

# Step 4: Create OASIS directory structure
echo -e "${BLUE}ðŸ“ Creating OASIS directory structure...${NC}"
mkdir -p ~/oasis/{config,logs,data}
cd ~/oasis

# Step 5: Download OASIS orchestrator (if available)
echo -e "${BLUE}â¬‡ï¸ Setting up OASIS orchestrator...${NC}"
echo "# OASIS v3 orchestrator will be placed here" > oasis_orchestrator.py

# Step 6: Create configuration
echo -e "${BLUE}âš™ï¸ Creating configuration...${NC}"
cat > oasis_config.json << 'EOF'
{
  "hf_token": "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",
  "endpoints": {
    "sentiment": "https://elmatador0197-my-oasis-agent.hf.space",
    "ai_factory": "https://your-username-oasis-ai-factory.hf.space"
  },
  "timeout": 30,
  "retry_attempts": 3,
  "retry_delay": 1.0
}
EOF

# Step 7: Create quick start script
echo -e "${BLUE}ðŸš€ Creating quick start script...${NC}"
cat > start_oasis.sh << 'EOF'
#!/bin/bash
echo "ðŸš€ Starting OASIS v3 Orchestrator..."
cd ~/oasis
python oasis_orchestrator.py
EOF

chmod +x start_oasis.sh

# Step 8: Verification
echo -e "${BLUE}ðŸ” Verifying installation...${NC}"
echo -e "${GREEN}âœ… Directory structure created${NC}"
echo -e "${GREEN}âœ… Configuration file created${NC}"
echo -e "${GREEN}âœ… Quick start script created${NC}"

# Final summary
echo ""
echo -e "${CYAN}ðŸŽ‰ OASIS v3 Termux Setup Complete!${NC}"
echo ""
echo -e "${YELLOW}ðŸ“Š Installation Summary:${NC}"
echo -e "  ðŸ“ Location: ~/oasis"
echo -e "  ðŸ’¾ Footprint: <1MB (pure Python)"
echo -e "  ðŸ”— Dependencies: ZERO external packages"
echo -e "  âš¡ Ready: Ultra-lightweight orchestrator"
echo ""
echo -e "${GREEN}ðŸš€ Next Steps:${NC}"
echo -e "  1. Copy your oasis_orchestrator.py to ~/oasis/"
echo -e "  2. Configure your HF endpoints in oasis_config.json"
echo -e "  3. Run: ./start_oasis.sh"
echo ""
echo -e "${PURPLE}Welcome to The New Civilization! ðŸŒŸ${NC}""
