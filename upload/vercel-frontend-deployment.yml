name: ▲ Vercel Frontend Deployment

on:
  push:
    branches: [main, develop]
    paths: ['frontend/**', 'package.json', 'next.config.js']
  pull_request:
    branches: [main]
    paths: ['frontend/**', 'package.json', 'next.config.js']
  workflow_dispatch:
    inputs:
      deploy_to_production:
        description: 'Deploy to production?'
        required: true
        default: false
        type: boolean

env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

jobs:
  build_and_test:
    name: 🏗️ Build & Test Frontend
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: 📋 Install Dependencies
        working-directory: ./frontend
        run: npm ci

      - name: 🧹 Lint Code
        working-directory: ./frontend
        run: |
          npm run lint
          npm run type-check

      - name: 🔬 Run Tests
        working-directory: ./frontend
        run: |
          npm run test:unit -- --coverage
          npm run test:e2e:headless

      - name: 🏗️ Build Application
        working-directory: ./frontend
        run: |
          npm run build
        env:
          NEXT_PUBLIC_HF_SPACES_URL: ${{ secrets.HF_SPACES_URL }}
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}

      - name: 📊 Bundle Analysis
        working-directory: ./frontend
        run: |
          npm run analyze

      - name: 💾 Cache Build
        uses: actions/cache@v3
        with:
          path: |
            frontend/.next
            frontend/node_modules
          key: ${{ runner.os }}-nextjs-${{ hashFiles('frontend/package-lock.json') }}

  deploy_preview:
    name: 🔍 Deploy Preview
    needs: build_and_test
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: 📦 Install Vercel CLI
        run: npm install --global vercel@latest

      - name: 🔗 Link Vercel Project
        working-directory: ./frontend
        run: vercel link --yes --token=${{ secrets.VERCEL_TOKEN }}

      - name: 🚀 Deploy to Preview
        id: deploy_preview
        working-directory: ./frontend
        run: |
          DEPLOYMENT_URL=$(vercel deploy --token=${{ secrets.VERCEL_TOKEN }})
          echo "deployment_url=$DEPLOYMENT_URL" >> $GITHUB_OUTPUT
          echo "✅ Preview deployed to: $DEPLOYMENT_URL" >> $GITHUB_STEP_SUMMARY

      - name: 🧪 Run Preview Tests
        run: |
          # Wait for deployment to be ready
          sleep 30

          # Run smoke tests against preview
          python scripts/ci/test_frontend_preview.py             --url "${{ steps.deploy_preview.outputs.deployment_url }}"             --timeout 120

      - name: 📝 Comment PR with Preview
        if: success()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## 🔍 Preview Deployment Ready!

              🚀 **Preview URL:** ${{ steps.deploy_preview.outputs.deployment_url }}
              📊 **Commit:** ${context.sha.substring(0, 7)}

              ### ✅ Tests Passed:
              - Build successful
              - Linting passed  
              - Unit tests passed
              - E2E tests passed
              - Preview deployment healthy

              You can test the changes directly in this preview environment.`
            })

  deploy_staging:
    name: 🧪 Deploy Staging
    needs: build_and_test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/develop' && github.event_name == 'push'
    environment: staging

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: 📦 Install Vercel CLI
        run: npm install --global vercel@latest

      - name: 🔗 Link Vercel Project
        working-directory: ./frontend
        run: vercel link --yes --token=${{ secrets.VERCEL_TOKEN }}

      - name: 🚀 Deploy to Staging
        id: deploy_staging
        working-directory: ./frontend
        run: |
          DEPLOYMENT_URL=$(vercel deploy --target=preview --token=${{ secrets.VERCEL_TOKEN }})
          echo "deployment_url=$DEPLOYMENT_URL" >> $GITHUB_OUTPUT
        env:
          NEXT_PUBLIC_ENVIRONMENT: staging
          NEXT_PUBLIC_HF_SPACES_URL: ${{ secrets.HF_SPACES_STAGING_URL }}

      - name: 🏷️ Set Staging Alias
        working-directory: ./frontend
        run: |
          vercel alias set ${{ steps.deploy_staging.outputs.deployment_url }} oasis-v3-staging.vercel.app             --token=${{ secrets.VERCEL_TOKEN }}

      - name: 🧪 Integration Tests
        run: |
          python scripts/ci/test_staging_integration.py             --frontend-url "https://oasis-v3-staging.vercel.app"             --backend-url "${{ secrets.HF_SPACES_STAGING_URL }}"             --timeout 300

  deploy_production:
    name: 🏭 Deploy Production
    needs: build_and_test
    runs-on: ubuntu-latest
    if: |
      (github.ref == 'refs/heads/main' && github.event_name == 'push') ||
      (github.event.inputs.deploy_to_production == 'true')
    environment: production

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: 📦 Install Vercel CLI
        run: npm install --global vercel@latest

      - name: 🔗 Link Vercel Project
        working-directory: ./frontend
        run: vercel link --yes --token=${{ secrets.VERCEL_TOKEN }}

      - name: 💾 Backup Current Production
        run: |
          python scripts/ci/backup_vercel_deployment.py             --project-id "$VERCEL_PROJECT_ID"             --backup-name "backup-$(date +%Y%m%d-%H%M%S)"

      - name: 🚀 Deploy to Production
        id: deploy_production
        working-directory: ./frontend
        run: |
          DEPLOYMENT_URL=$(vercel deploy --prod --token=${{ secrets.VERCEL_TOKEN }})
          echo "deployment_url=$DEPLOYMENT_URL" >> $GITHUB_OUTPUT
        env:
          NEXT_PUBLIC_ENVIRONMENT: production
          NEXT_PUBLIC_HF_SPACES_URL: ${{ secrets.HF_SPACES_PRODUCTION_URL }}

      - name: 🏥 Production Health Check
        id: health_check
        run: |
          python scripts/ci/health_check_frontend.py             --url "${{ steps.deploy_production.outputs.deployment_url }}"             --timeout 600             --critical

      - name: 🔄 Rollback on Failure
        if: failure() && steps.health_check.outcome == 'failure'
        run: |
          echo "🚨 Production deployment failed, initiating rollback..."
          python scripts/ci/rollback_vercel_deployment.py             --project-id "$VERCEL_PROJECT_ID"             --backup-name "$(python scripts/ci/get_latest_vercel_backup.py)"

      - name: 📊 Update Deployment Metrics
        if: success()
        run: |
          python scripts/ci/update_deployment_metrics.py             --platform "vercel"             --environment "production"             --deployment-url "${{ steps.deploy_production.outputs.deployment_url }}"             --commit-hash "${{ github.sha }}"

      - name: 📝 Production Summary
        if: success()
        run: |
          echo "🎉 Frontend production deployment successful!" >> $GITHUB_STEP_SUMMARY
          echo "🔗 URL: ${{ steps.deploy_production.outputs.deployment_url }}" >> $GITHUB_STEP_SUMMARY
          echo "📊 Commit: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "⏰ Deployed: $(date -Iseconds)" >> $GITHUB_STEP_SUMMARY

  lighthouse_audit:
    name: 🚦 Lighthouse Performance Audit
    needs: [deploy_staging, deploy_production]
    runs-on: ubuntu-latest
    if: always() && (needs.deploy_staging.result == 'success' || needs.deploy_production.result == 'success')

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🚦 Run Lighthouse CI
        uses: treosh/lighthouse-ci-action@v10
        with:
          urls: |
            https://oasis-v3-staging.vercel.app
            https://oasis-v3.vercel.app
          uploadArtifacts: true
          temporaryPublicStorage: true

      - name: 📊 Performance Report
        run: |
          python scripts/ci/analyze_lighthouse_results.py             --results-path ./lhci_reports             --threshold-performance 90             --threshold-accessibility 95

  security_scan:
    name: 🔒 Security Scan
    needs: build_and_test
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🔍 Run Snyk Security Scan
        uses: snyk/actions/node@master
        with:
          args: --severity-threshold=high --file=frontend/package.json
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      - name: 🛡️ OWASP ZAP Baseline Scan
        if: needs.deploy_staging.result == 'success'
        uses: zaproxy/action-baseline@v0.7.0
        with:
          target: 'https://oasis-v3-staging.vercel.app'
